---
title: "Test chunks"
author: "Test author"
format: html
---

```{r}
library(ggsql)
test_file <- "test.csv"
write.csv(mtcars, test_file)
```

```{ggsql}
SELECT mpg, disp FROM 'test.csv'
VISUALISE AS PLOT
DRAW point USING x = mpg, y = disp
```

```{r}
unlink(test_file)
```
